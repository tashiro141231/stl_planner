behavier_tree側からも実装してない部分ある
下のトピックを受けて、各モード実装
共通になる部分はプランナーベース側に実装
・ "stop_mode" 入力 std_msgs::Bool 
    True時→減速しながらアプローチ　
    False時→止まらずに同じ速度でアプローチ
・ "stop_navigation" 入力 std_msgs::Bool 
    True:V=0 W=0 にして、即座にアプローチ終了
    （減速方法は急ブレーキすると機体に悪いので変更するかも）
・ "$node$_navigation_state" 出力 
std_msgs::String Goal到達時に ”goal” を出力。
 Timeout 時は "timeout"
 （なんか拡張持たせるためにString）

//オブジェクト
 /stella_stereo/obstacle_2d


課題
速度変更
→1. プランナー側でROS param を毎回読みに行くいく変更
 2. BT側で逐次ROS paramを更新

一時停止後の高回転角速度対応
・一時停止後（Stop modeの次のwaypointに対して）のみ、角加速度を制限する
・角度だけ調整するプランナーを実装する？→PP側でStopMode中最後にWaypointの姿勢角に合わせに行く処理を追加 

BT側でTimeout後のDWAへ切り替え対応
・ほぼ実装済み
DWAテスト方針：
PPで障害物にアプローチさせて、タイムアウト後にDWAがよけれるか確認
　☆８月ごろにHILSで磯村とデバッグしてみる
スタートの時のJoyバグ対応